---
name: time-tracker
version: 3.24.0
description: 柳比歇夫时间统计法。对话中用户说事件名即记录时间节点（自动结束上一个、开始新的），支持修正名称、给进行中事件追加备注；日/周/月/季/年统计通过导出周期内原始事件文本后由模型当场分析。当用户声明本对话用于时间统计、说事件名表示切换任务、要求统计时间花费、查看当前在做什么时使用。
---

# time-tracker

脚本：`<skill_dir>/scripts/time_tracker.py`，统一用 `python3 time_tracker.py <命令>` 调用。
数据库读写、飞书同步由脚本自动处理，模型无需关心。

## 命令

| 命令 | 作用 |
|------|------|
| `current` | 看当前在做什么（含备注） |
| `start "<事件名>" [--rename "<新名>"] [--note "<备注>"]` | 结束上一个事件并开始新事件 |
| `stop` | 结束当前事件（自动转"未记录"） |
| `rename-current "<新名>"` | 改当前进行中事件的名 |
| `note "<备注>"` | 给当前进行中的事件追加备注（流水式，用；拼接） |
| `note --clear` | 清空当前事件的备注 |
| `list-events [--limit N]` | 看最近记录（带ID） |
| `rename-event "<事件ID>" "<新名>"` | 改已结束事件的名（少用） |
| `export-period <周期> [YYYY-MM-DD]` | 导出周期内原始事件文本，用于统计 |
| `export` | 导出全部原始事件 JSON |

`<周期>`：`day` / `week` / `month` / `quarter` / `year`，默认今天。

## 记录时怎么解析用户输入

- 整句就是事件名：直接 `start "<整句>"`。
- 句中含"其实是在X / 不是X而是Y / 应该叫X"：上一个事件名用 `--rename "X"`。
- 句中含"期间还做了X / 顺便X"（指上一段已结束的事）：给上一个事件加 `--note "X"`。
- 句中含"记一下 / 备注 / 补充：X"（指正在做的事）：**不切换事件**，直接 `note "X"`。
- 两者可同时用。无以上信号，整句就是新事件名。
- 回复用户时直接转述脚本输出，带上一个事件花了多久；加备注后带上当前备注全文。

## 统计

用户要统计某周期：跑 `export-period <周期> [日期]`，把它打印的原始事件文本读完，自行计算时长合计、按事件汇总、未记录缺口、备注回顾等，直接回答用户。统计不落库，每次都从原始文本现算。
