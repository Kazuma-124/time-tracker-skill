---
name: time-tracker
version: 3.21.0
description: 柳比歇夫时间统计法追踪工具。在云电脑工作模式对话中，通过时间节点法记录事件起止时间，自动计算时长，并提供日/周/月/季度/年度统计、事件平均时长查询、事件名称与分类管理、季度分类评审。当用户声明本对话用于时间统计、输入事件名称表示切换任务、要求统计时间花费、查询做某事通常多久、管理事件名称或分类、季度评审时使用。数据持久化在 SQLite 数据库，对话仅作为输入接口。
---

# 柳比歇夫时间统计法 (time-tracker)

## 核心原理

时间节点法：每次输入下一个事件名称 = 创建一个时间节点，同时完成两件事：
1. **结束上一个事件**：记录结束时间，自动计算持续时长
2. **开始下一个事件**：记录开始时间

事件名称是唯一输入，无需手动输入时间。

## 脚本路径

```
<script_dir>/scripts/time_tracker.py
```

所有命令通过 `python3 <script_path> <command>` 调用。

**数据存储位置**：SQLite 数据库文件 `~/workspace/time-tracker-data/time-tracker.db`（与技能代码分离）。可通过环境变量 `TIME_TRACKER_DATA_DIR` 覆盖数据目录。

## 命令总览

| 命令 | 用途 |
|------|------|
| `start "<事件名>" [--rename "<修正名>"] [--extras "<描述>"]` | 记录时间节点（结束上一个 + 开始下一个） |
| `stop` | 结束当前事件，自动开始"未记录" |
| `current` | 查看当前进行中的事件 |
| `rename-current "<新名称>"` | 修正当前进行中的事件名称 |
| `list-events [--limit N]` | 列出最近的事件记录（带ID） |
| `rename-event "<事件ID>" "<新名称>"` | 强制修改已完成事件的名称（非常规操作） |
| `stats <周期> [日期]` | 统计分析（文本输出） |
| `stats-json <周期> [日期]` | 统计分析（结构化JSON输出，供可视化/报告） |
| `query "<事件名>"` | 查询某事件的历史平均时长 |
| `name-check --period <周期> [日期]` | 检查事件名称与分类，输出管理方案 |
| `name-stats` | 输出所有事件名称统计（表格形式，含基本分析指引） |
| `aliases` | 列出所有原始名→标准名映射 |
| `add-alias "<原始名>" "<标准名>"` | 添加原始名映射 |
| `remove-alias "<原始名>"` | 删除原始名映射 |
| `standard-names` | 列出所有标准名及其分类 |
| `add-standard-name "<名称>" --category "<分类>" [--description "..."]` | 创建新的标准名（分类必填，分类名可与标准名相同） |
| `set-standard-category "<标准名>" "<新分类>"` | 修改标准名的分类 |
| `categories` | 列出所有分类（树形结构） |
| `add-category "<名称>" [--parent "<父分类>"] [--description "..."]` | 添加分类 |
| `remove-category "<名称>"` | 删除分类 |
| `quarterly-review` | 季度分类评审（输出候选方案） |
| `export` | 导出原始事件 JSON |
| `export-stats <周期> [日期] [--format json\|csv] [--output 文件名]` | 导出统计结果供下载 |
| `init-test-db` | 初始化测试数据库（从生产数据库复制一份用于测试） |

## 使用流程

### 1. 对话初始化

当用户声明"本对话用于时间统计"或类似意图时：
- 确认技能已加载
- 告知用户：之后直接输入事件名称即可记录时间节点
- 查看当前状态：`python3 <script> current`

### 2. 记录时间节点（最常用）

用户输入有两种格式，模型需正确解析：

**格式一：只有事件名称（默认无额外事情）**
```
用户输入：吃饭
```
执行：
```bash
python3 <script> start "吃饭"
```

**格式二：先描述/修正上一个事件，再声明新事件**
```
用户输入：刚才其实是在看视频不是写代码，期间还吃了零食。开始新事件：吃饭
```
模型解析出对上一个事件的修正和描述，执行：
```bash
python3 <script> start "吃饭" --rename "看视频" --extras "期间还吃了零食"
```

**解析规则**：
- 用户输入中如果包含"开始新事件"、"接下来"、"然后"等显式切换信号，信号之前的内容是对当前将要结束的事件的描述
- 模型需要判断描述内容的类型：
  - **名称修正**：用户说"其实是在X"、"不是X而是Y"、"应该叫X"等 → 有两种处理方式：
    - 方式一（推荐）：先调用 `rename-current "X"` 修正当前事件名称，再正常 start 新事件
    - 方式二：在 start 新事件时使用 `--rename "X"` 参数，在结束时修正
  - **额外事情**：用户说"期间还做了X"、"顺便做了X"等 → 使用 `--extras` 记录
  - 两者可以同时存在
- 如果没有显式切换信号，整个输入就是新事件名称
- 修正后的名称如果是已配置的原始名，会自动解析为标准名
- 使用 `--rename` 时，原始名称会保存在 extras 字段中（格式："（原始名：xxx）"），保证可追溯
- 使用 `rename-current` 时，current 表直接更新，开始时间不变

**设计原则**：
- current 表（当前进行中的事件）可以修改名称（分类通过映射实时获取）
- events 表（已完成的事件）只追加不修改，保证可追溯
- 事件进行中发现名称不对 → 用 `rename-current` 立即修正
- 事件结束时才发现名称不对 → 用 `--rename` 在结束时修正

**关键行为**：
- 如果有进行中的事件，自动结束它并输出时长
- 然后开始新事件
- 记录事件时只保存原始名，不检查标准名映射；映射管理在统计任务（name-check）中由模型进行
- 完整数据同步流程：操作前从飞书拉取最新数据库 → 本地修改 → 上传到飞书 → 删除本地临时数据库（详见"数据同步机制"章节）
- 数据不依赖对话上下文，所有对话共享飞书上的同一份数据

**回复用户时**：必须突出展示上一个事件的花费时间，以增强用户的时间把控感。推荐格式：
```
✅ 上一个事件「写代码」已结束
   时间：09:00 ~ 13:00
   花费：4 小时 0 分钟

▶ 现在开始「吃饭」
   开始时间：13:00
```

### 3. 结束当前事件

```bash
python3 <script> stop
```

stop 会结束当前事件，并自动开始"未记录"事件，保证时间连续性。

## 统计功能

### 命令格式

```bash
python3 <script> stats <period> [date]
```
- `period`: `day` / `week` / `month` / `quarter` / `year`
- `date`: 可选，参考日期 `YYYY-MM-DD`，默认今天

### 统计内容

- **未记录自动补全**：用"未记录"虚拟事件补满时间周期（日=24h，周=168h，月/季/年=天数×24h），总记录时长恒等于周期总时长
- 显示实际记录时长与未记录时长
- **按分类**：总时长、占比（占已记录时间比例 = 占周期总时长比例）
- **按事件**：总时长、次数、平均时长、占比（Top 20，应用原始名→标准名映射）
- 周/月/季/年统计额外包含**每日实际记录时长**（标注 / 24h）
- **日统计后应自动运行 name-check** 进行名称与分类管理

### 未记录设计

基于柳比歇夫法"记录全天24小时"的理念，统计时自动计算未记录时长并作为虚拟事件补满周期：
- 未记录时长 = 周期总时长 - 实际记录时长
- 虚拟事件名称为"未记录"，分类为"未记录"
- 仅存在于统计输出中，不写入数据库
- 占比基于补满后的总时长计算

### 跨天事件处理

事件跨越零点时，日统计会按天自动拆分计算。

### 结构化 JSON 输出（stats-json）

```bash
python3 <script> stats-json <period> [date]
```

输出与 `stats` 命令完全一致的数据，但格式为结构化 JSON，供以下场景消费：
- **可视化技能**：直接提取 `by_category` 做饼图、`daily` 做折线图
- **定时任务报告**：模型读取 JSON 后整理为报告
- **数据分析**：其他工具或脚本消费统计数据

**输出字段说明**：
```json
{
  "period": "day",
  "start": "2026-09-01T00:00:00+08:00",
  "end": "2026-09-02T00:00:00+08:00",
  "total_minutes": 1440,
  "actual_minutes": 840,
  "unrecorded_minutes": 600,
  "event_count": 5,
  "by_category": [{"name": "研发", "minutes": 240, "percentage": 16.7}],
  "by_event": [{"name": "写代码", "minutes": 240, "count": 1, "average_minutes": 240, "percentage": 16.7}],
  "daily": [{"date": "2026-09-01", "minutes": 840, "by_category": {"研发": 240}}],
  "has_data": true
}
```

### 导出统计报告（export-stats）

所有统计均为临时计算（从原始数据实时聚合），不做持久化缓存。用户可主动将统计结果导出为文件供下载：
```bash
python3 <script> export-stats <period> [date] [--format json|csv] [--output 文件名]
```
- **JSON**：与 `stats-json` 输出完全一致
- **CSV**：多 section 表格，UTF-8 BOM 编码，可直接用 Excel 打开
- 输出位置：数据目录下的 `exports/` 子目录

## 事件名称与分类管理

### 核心规则

1. **原始名都必须映射到一个标准名**（必须通过 `aliases` 表显式建立映射，即使原始名与标准名相同也要建立，如"睡觉"→"睡觉"）
2. **标准名都必须映射到一个分类名**（通过 `standard_names.category_id`，即使分类名与标准名相同也要建立，如标准名"娱乐"→分类名"娱乐"）
3. **标准名应该能准确描述所涵盖的原始名表示的事件，且只能表示一个事件**
4. **特殊事件"未记录"**映射到特殊标准名"未记录"，归为特殊分类"未记录"
5. **命名唯一性规则**：
   - 标准名之间不能相同
   - 分类名之间不能相同
   - 标准名与分类名之间可以相同（本质不是同一类对象）
   - 原始名与标准名之间可以相同（需显式建立同名映射）
6. **所有标准名、分类名、映射关系由模型决定**，脚本不自动创建

### 三层映射结构

```
用户输入原始名（如"歇会"）
    ↓ aliases 表
标准名（如"短暂休息"）
    ↓ standard_names.category_id
分类名（如"娱乐"）
    ↓ categories.parent_id（可选）
父分类（多级分类）
```

### 每日统计时的自动管理

每日统计（`stats day`）后，模型应自动运行 `name-check` 进行名称与分类管理：

```bash
python3 <script> name-check --period day
```

`name-check` 输出：
- 本周期事件名称列表
- 历史名称库完整统计（次数、总时长、首次/末次出现）
- 已有原始名映射
- **未映射检查**：哪些原始名未映射到标准名（必须处理）
- **标准名与分类**：所有标准名及其分类，标记未分类的标准名（必须处理）
- 模型分析指引和输出格式

**模型分析维度**：
1. **未映射的原始名**：分析应映射到哪个已有标准名，或需要创建新的标准名
2. **未分类的标准名**：分析应归到哪个已有分类，或需要创建新的分类
3. **相似名称映射分析**：判断是否描述同一事件，若是则建立原始名→标准名映射
4. **标准名准确性**：现有标准名是否能精确描述事件，是否需要改名或拆分
5. **分类合理性**：分类是否需要调整、合并、拆分或改名
6. **已有映射调整**：根据更完整的数据，修改或移除不合适的已有映射

**模型输出格式**（结构化 JSON）：
```json
{
  "new_standard_names": [{"name": "标准名", "category": "分类名", "reason": "理由"}],
  "new_categories": [{"name": "分类名", "parent": "父分类名或null", "reason": "理由"}],
  "new_mappings": [{"raw_name": "原始名", "standard_name": "标准名", "reason": "理由"}],
  "category_changes": [{"standard_name": "标准名", "new_category": "新分类名", "reason": "理由"}],
  "mapping_changes": [{"raw_name": "原始名", "old_standard": "旧标准名", "new_standard": "新标准名", "reason": "理由"}],
  "removals": [{"type": "mapping|standard_name|category", "name": "名称", "reason": "理由"}]
}
```

**重要**：模型只输出建议方案，不自动执行。用户确认后用对应命令执行。

### 执行命令

- 创建标准名：`python3 <script> add-standard-name "<名称>" --category "<分类>"`
- 创建分类：`python3 <script> add-category "<名称>" --parent "<父分类>"`
- 添加原始名映射：`python3 <script> add-alias "<原始名>" "<标准名>"`
- 修改标准名分类：`python3 <script> set-standard-category "<标准名>" "<新分类>"`
- 移除映射：`python3 <script> remove-alias "<原始名>"`
- 查看所有映射：`python3 <script> aliases`
- 查看所有标准名：`python3 <script> standard-names`

### 设计原则

- **不使用字符串相似度算法**（包含关系、前缀匹配、编辑距离等），完全依赖模型的语义理解能力
- **原始数据不修改**：`events` 表中仍保留原始名称，确保数据可追溯
- `start` 时只保存原始名，不检查或创建标准名映射；统计时通过 name-check 由模型管理映射

## 查询功能

```bash
python3 <script> query "<事件名称>"
```

支持模糊匹配（应用原始名→标准名映射）。输出：
- 匹配次数、总时长
- 平均时长、中位数时长、最短、最长
- 分类分布
- 最近 5 次记录

## 分类管理

### 设计原则

- **花费时间长或规律固定的事件** → 创建独立分类
- **临时性或短时间事件** → 统一归入"其他"
- 每季度评审一次，调整分类结构

### 多级分类

分类支持树形结构（通过 `parent_id` 实现）：
- 一个分类可以有父分类
- 一个分类下可以包含事件（通过标准名关联）和子分类
- 分类不能出现环路
- 一个标准名只能属于一个分类

### 命令

- 查看：`python3 <script> categories`（树形展示）
- 添加：`python3 <script> add-category "<分类名>" --parent "<父分类>" --description "<说明>"`
- 删除：`python3 <script> remove-category "<分类名>"`（不影响历史记录）

## 季度分类评审

### 命令

```bash
python3 <script> quarterly-review
```

### 两步流程

**第一步：事件名称一致性检查**
- 检查本季度所有事件名称与历史名称库
- 输出可能需要建立原始名→标准名映射的名称对
- 先解决命名问题，再讨论分类策略

**第二步：分类调整候选方案**

输出三类候选方案，每类方案标注推荐级别（强烈推荐/建议考虑/可选）：

| 方案类型 | 检测逻辑 |
|---------|---------|
| **A. 新建分类** | "其他"中某事件占比高或绝对时长长 |
| **B. 合并分类** | 某分类占比低且绝对时长短 |
| **C. 拆分分类** | 某分类内Top1事件占比过高且事件种类多 |

### 用户决策

评审只输出候选方案，**不自动执行**。用户审查后：
- 保留的方案：使用 `add-category` / `remove-category` 手动执行
- 弃用的方案：忽略即可
- 调整的方案：修改分类名后执行

## 数据结构

### 数据库

数据存储在 SQLite 数据库文件中（默认 `~/workspace/time-tracker-data/time-tracker.db`，可通过 `TIME_TRACKER_DATA_DIR` 环境变量覆盖）。

使用 WAL 日志模式，支持事务保证数据一致性。events 表只 INSERT 不 UPDATE/DELETE，保证原始数据可追溯。

### 表结构

| 表名 | 用途 | 关键字段 |
|------|------|---------|
| `events` | 原始事件记录（权威数据源，只追加，只存原始名和时间） | id, name, start_time, end_time, duration_minutes, extras |
| `current` | 当前进行中的事件（单行） | name, start_time |
| `categories` | 分类配置（支持多级） | id, name, parent_id, description, created_at |
| `aliases` | 原始名→标准名映射 | alias, standard_name |
| `standard_names` | 标准名及其分类 | id, name, category_id, description, created_at, updated_at |

### 索引

- `idx_events_start` ON events(start_time) — 按时间范围查询加速
- `idx_events_name` ON events(name) — 按事件名称查询加速
- `idx_aliases_standard` ON aliases(standard_name) — 原始名反向查询加速
- `idx_standard_names_category` ON standard_names(category_id) — 按分类查询标准名加速

#
## 数据同步机制

### 核心原则
**混合存储架构**：
- **技能代码**（SKILL.md、脚本、config）存储在 GitHub 私有仓库 `Kazuma-124/time-tracker-skill`，通过 GitHub MCP 连接器进行版本控制和同步
- **数据库文件**（SQLite）存储在飞书云空间，作为唯一的权威数据源
- 本地数据库只是临时工作区，操作完成后立即删除

### 技能代码同步
技能代码通过 time-tracker-launcher 从 GitHub 拉取，**统一使用 GitHub MCP 连接器**，禁止使用 git 命令行或其他方式：
- 调用 `mcp__github__get_file_contents` 逐个拉取仓库文件到本地
- 对比 VERSION 文件，版本一致时跳过
- 详细流程见 time-tracker-launcher/SKILL.md
- 修改技能后通过 `mcp__github__create_or_update_file` 上传到 GitHub

### 核心原则：失败即报错停止

**任何环节失败时，直接报错并停止当前操作，绝不降级继续或使用可能过时/损坏的数据。**

| 失败场景 | 处理方式 |
|---------|---------|
| 配置文件加载失败 | 报错停止，不使用默认值兜底 |
| 从飞书拉取数据库失败 | 报错停止，不使用本地旧数据 |
| 本地数据库完整性检查失败 | 尝试从飞书恢复，恢复失败则报错停止 |
| 获取数据库锁失败 | 报错停止，不并发写入 |
| 备份到飞书失败 | 保留本地数据库，报错提示手动备份 |
| 本地记录数过少且飞书有备份 | 拒绝上传并报错，防止空数据覆盖 |
| 上传后验证失败 | 删除刚上传的损坏文件，重试，最终失败则报错 |

### 数据库同步（每次操作的完整流程）

```
用户执行命令（如 start / stats / add-alias 等）
    ↓
1. 从飞书拉取最新数据库到本地
   - 调用 backup_to_lark.py --restore
   - 下载飞书上最新的数据库备份
   - 覆盖本地临时数据库
   - 拉取失败时：直接报错停止，不使用本地旧数据（防止操作过时数据）
    ↓
2. 在本地执行操作（查询或修改）
   - 只读操作：直接查询本地数据库
   - 修改操作：在本地事务中修改数据库
    ↓
3. 修改操作：上传到飞书
   - 调用 backup_to_lark.py（无参数，仅备份数据库）
   - 上传本地数据库到飞书
   - 验证上传成功（下载回来检查完整性）
   - 清理飞书上的旧版本文件
    ↓
4. 删除本地临时数据库
   - 删除数据库文件、WAL、SHM、锁文件
   - 确保本地不残留任何数据库文件
```

### 为什么这样设计

| 设计决策 | 原因 |
|---------|------|
| 飞书是唯一权威数据源 | 云电脑环境会重置，不同对话环境隔离，必须有统一数据源 |
| 每次操作前拉取 | 保证任何对话操作的都是最新数据，避免环境隔离导致数据不同步 |
| 每次修改后上传 | 保证其他对话能立即看到最新数据 |
| 操作后删除本地数据库 | 避免本地旧数据残留，防止下次操作误用旧数据 |
| 上传后验证 | 防止上传失败导致数据丢失 |

### 注意事项
- 测试模式（`--test`）下不会删除测试数据库，也不会上传到飞书
- `init-test-db` 命令不会删除本地数据库（它的目的就是创建测试数据库）
- 拉取或上传失败时直接报错停止，不会静默失败或降级继续

## 事件记录字段

```json
{
  "id": "短ID",
  "name": "原始事件名称",
  "start_time": "ISO8601",
  "end_time": "ISO8601",
  "duration_minutes": 123.5,
  "extras": "额外描述（可选）"
}
```

> **注意**：events 表不存储分类字段，分类通过原始名→标准名→分类的映射实时获取。

## 数据备份与恢复

### 背景

云电脑环境可能会重置，导致本地数据库文件丢失。为防止数据丢失，提供备份到飞书云空间的功能。

### 技能版本标识与同步

技能目录下包含 `VERSION` 文件，记录当前技能版本号（语义化版本：主.次.修订）。

**版本号更新规则**：
- **主版本号**：不兼容的架构修改
- **次版本号**：向下兼容的功能性新增
- **修订号**：向下兼容的问题修正

**每次修改技能后，必须更新 VERSION 文件中的版本号，然后通过 GitHub MCP 连接器上传**：

1. 修改技能文件
2. 更新 VERSION 文件中的版本号
3. 对每个修改的文件，调用 `mcp__github__get_file_contents` 获取当前 SHA
4. 调用 `mcp__github__create_or_update_file` 上传新内容（含 SHA 和 commit message）

**禁止使用 git commit + push**，统一通过 MCP 连接器上传。

技能代码仓库：`Kazuma-124/time-tracker-skill`（私有，分支 main）

> **注意**：技能代码不再备份到飞书云空间，GitHub 是技能代码的唯一存储和版本控制平台。数据库仍备份到飞书云空间。

### 数据库初始化流程

**本地数据库不存在时**：
1. 尝试从飞书云空间恢复最新数据库
2. 恢复成功 → 使用恢复的数据库
3. 恢复失败且无本地数据库 → 直接报错，绝不创建空数据库（防止空数据覆盖飞书备份）

### 进行中事件的备份与恢复

进行中的事件（current 表）也会随数据库一起备份和恢复，这是跨对话连续记录的关键：

```
对话A中开启事件"工作"
    ↓
事件记录在 current 表中（进行中状态）
    ↓
备份数据库到飞书（包含 current 表）
    ↓
删除对话A → 环境A销毁
    ↓
对话B初始化 → 从飞书恢复数据库
    ↓
current 表中的"工作"事件正确恢复
    ↓
用户开启新事件 → 自动结束"工作"，正确计算持续时间
```

**对话初始化时必须先恢复**：新对话开始时，先执行 `current` 命令触发数据库恢复。

### 统一数据库修改入口（db_transaction）

所有对数据库有写入操作的命令（start、stop、rename-current、rename-event、add-alias、remove-alias、add-category、remove-category、add-standard-name、set-standard-category）都通过统一的 `db_transaction` 上下文管理器执行，保证完整流程：

1. **拉取**：从飞书拉取最新数据库覆盖本地
2. **修改**：在本地执行数据库修改操作
3. **上传**：将修改后的数据库上传到飞书
4. **验证**：下载上传的文件验证完整性
5. **清理**：验证成功后删除飞书上的旧版本
6. **重试**：上传失败则删除损坏文件并重试（最多3次）

只读命令（stats、current、list-events、query、name-stats 等）仍使用 `init_db()` + `get_db()`，只拉取不修改。

### 自动备份机制

备份已集成到 `db_transaction` 中，每次写入操作后自动执行，无需单独调用。

**为什么不用定时任务**：
- 云电脑环境在不同对话之间是隔离的
- 定时任务触发时在全新的独立环境中执行，无法访问用户对话环境中的数据库
- 所以必须在每次操作后即时备份

### 备份脚本

```bash
# 备份数据库到飞书
python3 <script_dir>/backup_to_lark.py

# 强制备份数据库（跳过安全检查）
python3 <script_dir>/backup_to_lark.py --force

# 从飞书恢复数据库
python3 <script_dir>/backup_to_lark.py --restore

# 清理飞书上的重复/损坏数据库备份文件
python3 <script_dir>/backup_to_lark.py --cleanup
```

> **注意**：统计任务配置（`config/stats-task.json`）已包含在技能代码中，随技能一起从 GitHub 拉取，无需单独备份。

**数据库备份文件名**：`time-tracker-db-{最新事件完成时间}.db`（如 `time-tracker-db-20260903_010643.db`）

**安全检查（防止数据丢失）**：
- 备份前检查本地数据库的事件记录数
- 如果本地记录数为 **0** 且飞书已有备份 → **拒绝上传并报错**，防止空数据库覆盖飞书数据
- 如果本地记录数少于 **3** 条且飞书已有备份 → **拒绝上传并报错**，防止数据异常
- 使用 `--force` 参数可强制跳过安全检查（不推荐）

### 数据恢复

需要恢复数据时，直接运行恢复命令：
```bash
python3 <skill_dir>/scripts/backup_to_lark.py --restore
```

恢复脚本会自动：
1. 从飞书云空间下载最新的数据库备份
2. 验证下载的文件是否为有效的 SQLite 数据库
3. 如果本地已有数据库，先备份为 `.bak.时间戳` 文件
4. 替换本地数据库
5. 显示恢复的记录数

### 飞书云空间信息

- 备份根文件夹：「时间统计备份」
- 子文件夹：「数据库备份」（仅存储数据库文件，技能代码已迁移到 GitHub）

## 定时任务集成

各功能均为无状态命令行调用，可直接用于定时任务：
- **每日统计**：`python3 <script> stats day`（建议每天 23:59）
- **每周统计**：`python3 <script> stats week`（建议每周日 23:59，日统计之后）
- **每月统计**：`python3 <script> stats month`（建议每月最后一天 23:59）
- **季度评审**：`python3 <script> quarterly-review`（建议每季度最后一天）
- **年度统计**：`python3 <script> stats year`（建议每年最后一天）

若同一天有多项统计，按优先级执行：日 > 周 > 月 > 季度 > 年。

定时任务触发后，模型应：
1. 从 GitHub 拉取最新技能代码（通过 GitHub MCP 连接器）
2. 从飞书恢复最新数据库
3. 执行统计
4. 运行 name-check 进行名称与分类管理
5. 整理报告发送给用户

## 注意事项

1. **时区**：所有时间使用 Asia/Shanghai (UTC+8)
2. **数据安全**：原始事件数据只追加不修改，原始名→标准名映射仅影响统计显示
3. **首次使用**：第一次 `start` 时没有上一个事件，仅开始新事件
4. **环境重置**：云电脑环境可能会重置，导致本地数据丢失。每次数据库操作后会自动备份到飞书云空间
5. **名称管理**：所有标准名、分类名、映射关系由模型决定，脚本不自动创建。每日统计时应自动进行名称与分类检查
6. **时间连续性**：任何时刻都应有事件在进行中。stop 后自动开始"未记录"，确保时间不中断


## 测试数据库隔离

### 背景
模型在测试技能功能时，如果直接操作生产数据库，可能会误创建事件或修改数据，影响真实记录。

### 使用方法

**第一步：初始化测试数据库**
```bash
python3 scripts/time_tracker.py init-test-db
```
从生产数据库复制一份到测试数据库路径（`test-time-tracker.db`）。

**第二步：使用测试模式**
在任何命令前添加 `--test` 参数，所有操作将指向测试数据库：
```bash
python3 scripts/time_tracker.py --test current
python3 scripts/time_tracker.py --test start "测试事件"
python3 scripts/time_tracker.py --test stats day
```

### 测试模式特性
- **数据隔离**：测试数据库与生产数据库完全独立，修改测试数据不影响生产数据
- **不备份到飞书**：测试模式下不会备份到飞书，避免测试数据污染生产备份
- **不从飞书拉取**：测试模式下不会从飞书拉取最新数据，直接使用本地测试数据库
- **可重复初始化**：运行 `init-test-db` 会重新从生产数据库复制，覆盖旧的测试数据库

### 注意事项
1. 测试功能时必须使用 `--test` 参数，禁止直接操作生产数据库
2. 测试完成后可删除测试数据库文件，或保留以备后续测试
3. 生产数据库的操作不受测试模式影响
